version: 0.1
python: 3.6
entrypoint: python runway_model.py
cuda: 10.2
framework: pytorch
spec:
    cpu: False
    gpu: True
files:
    ignore:
        - "*.pt"
        - "*.dat"
build_steps:
    - pip install runway-python==0.6.1
    - pip install -r requirements.txt
